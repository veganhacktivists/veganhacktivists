on:

  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'
      tags:
        description: 'Test scenario tags'

env:
  REGISTRY: ${{ secrets.PRIVATE_REGISTRY }}
  IMAGE_NAME: ${{ github.event.repository.name }}
  HUSKY_SKIP_INSTALL: 1

jobs:

  build_and_push:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - name: Checkout main
        uses: actions/checkout@v2

      - name: Build container image
        run: docker build -t $(echo $REGISTRY)/$(echo $IMAGE_NAME):latest .

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          registry: db-admin.veganhacktivists.org
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Push image to Container Registry
        run: docker push $(echo $REGISTRY)/$(echo $IMAGE_NAME):latest

  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    needs: build_and_push

    steps:
       - name: Deploy to Digital Ocean droplet via SSH action
         uses: appleboy/ssh-action@v0.1.3
         with:
           HOST: ${{ secrets.HOST }}
           USERNAME: ${{ secrets.USERNAME }}
           KEY: ${{ secrets.SSH_SECRET_KEY }}
           envs: IMAGE_NAME,REGISTRY,GITHUB_SHA,COMMAND
           script: |
             docker stop $(echo $IMAGE_NAME)
             docker rm $(echo $IMAGE_NAME)
             docker run -d \
             --restart always \
             --label traefik.enable=true \
             --label traefik.http.services.$(echo $IMAGE_NAME).loadbalancer.server.port=80 \
             --label traefik.http.routers.$(echo $IMAGE_NAME)-http.entrypoints=http \
             --label traefik.http.routers.$(echo $IMAGE_NAME)-http.rule=Host(`new.veganhacktivists.org`) \
             --label traefik.docker.network=traefik-public \
             --label traefik.http.routers.$(echo $IMAGE_NAME)-https.entrypoints=https \
             --label traefik.http.routers.$(echo $IMAGE_NAME)-https.rule=Host(`new.veganhacktivists.org`) \
             --label traefik.http.routers.$(echo $IMAGE_NAME)-https.tls=true \
             --label traefik.http.routers.$(echo $IMAGE_NAME)-https.tls.certresolver=le \
             --label traefik.http.middlewares.https-redirect.redirectscheme.scheme=https \
             --label traefik.http.middlewares.https-redirect.redirectscheme.permanent=true \
             --label traefik.http.routers.$(echo $IMAGE_NAME)-http.middlewares=https-redirect \
             --network=traefik-public
             --name $(echo $IMAGE_NAME) \
             $(echo $REGISTRY)/$(echo $IMAGE_NAME):latest
