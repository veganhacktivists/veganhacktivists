import React from 'react';
import type { ReactElement } from 'react';

const Main = (): ReactElement => {
  return <main id="main" className="text-center min-h-[40rem]" tabIndex={-1} />;
};
export default Main;
