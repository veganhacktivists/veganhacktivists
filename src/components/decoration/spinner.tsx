import { faSpinner } from '@fortawesome/free-solid-svg-icons';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const Spinner: React.FC = () => <FontAwesomeIcon icon={faSpinner} spin />;

export default Spinner;
